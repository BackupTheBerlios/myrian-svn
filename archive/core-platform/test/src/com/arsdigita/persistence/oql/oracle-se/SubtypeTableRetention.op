        do {
 select sup.sup_attribute as sup__sup_attribute,
        sup.sup_id as sup_id
   from sub sub,
        sup sup
  where sup.sup_id = sub.sub_id
        } map {
            supAttribute = sup.sup__sup_attribute;
            id = sup.sup_id;
        }
