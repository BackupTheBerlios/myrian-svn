select s1.sup_id as column0,
       required__r.ref_id as column1,
       required__r.ref_attribute as column2
from sub s
     join sup s1 on s1.sup_id = s.sub_id
     join refs required__r on required__r.ref_id = s.required_id
